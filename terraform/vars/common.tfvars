gcp_default_region = "australia-southeast2"
gcp_default_zone   = "australia-southeast2-a"

# docker repositories is a list of strings that contains names for repositories to create
# currently used in docker_repositories root module
docker_repositories = ["store"]